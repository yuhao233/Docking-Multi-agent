# Docker 构建时的 pip 源（langgraph.json 的 pip_config_file 指向本文件）
# 与 pyproject.toml 的 uv 源一致：内网/国内拉包更快，也避免构建超时。
[global]
index-url = https://mirrors.aliyun.com/pypi/simple/
trusted-host = mirrors.aliyun.com
timeout = 60
